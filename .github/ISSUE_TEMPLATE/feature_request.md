---
name: Feature request
about: New API or behavior
labels: enhancement
---

## Is this termbox2 parity?
If yes: cite the `ROADMAP.md` row (the gap may already be tracked, with
notes on why it's `[~]`/`[ ]`).

## Motivation
What can't you do today?

## Proposed shape
API sketch if you have one (`lt_*` naming; must be implementable on both
POSIX and Windows, or explicitly platform-gated like `lt_get_fds`).

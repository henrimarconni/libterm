# Session: sgr-posix-color-modes

- [x] 1. Add failing byte-level tests for mode-specific SGR output.
- [x] 2. Implement `LT_OUTPUT_256` and `LT_OUTPUT_TRUECOLOR` SGR emission on POSIX.
- [x] 3. Implement `LT_OUTPUT_216` and `LT_OUTPUT_GRAYSCALE` mapping + emission on POSIX.
- [x] 4. Optimize mode-aware SGR emission and verify full test suite.
